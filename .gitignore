# 原本（scripts/00_download.py で再取得できるため追跡しない）
data/raw/

# Geolonia 住所マスタ（scripts/02_fetch_address_master.py で再取得可）
# cache は約147MB、master_town.csv は約52MB あるので追跡しない。
# master_city.csv（約100KB）は突合の確認に有用なので追跡する。
data/address_master/cache/
data/address_master/master_town.csv

# N03 行政区域（scripts/09_pdf_truth.py が再取得可・都道府県あたり数十MB）
data/n03/

# 中間生成物（scripts/03〜05 で再生成可）
work/

# Node
node_modules/

# 一時ファイル
*.pyc
__pycache__/
.DS_Store
Thumbs.db

# QGIS検証用 GeoPackage（scripts/08_export_for_qgis.py で再生成可・62MB）
output/f9_groundwater_wells_qgis.gpkg

# 正解データの GeoPackage（scripts/09_pdf_truth.py --gpkg で再生成可。CSVを追跡する）
output/f9_pdf_truth_2012.gpkg
